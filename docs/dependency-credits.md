# Local dependency notes

CampusHub's certificate PDFs are produced by the project's small purpose-built writer in `includes/certificate.php`. It has no Composer, CDN, external API, or other third-party runtime dependency.
